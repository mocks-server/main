{
  "private": true,
  "scripts": {
    "mocks": "mocks-server",
    "start": "mocks-server --no-plugins.inquirerCli.enabled"
  },
  "devDependencies": {
    "@mocks-server/main": "workspace:*"
  }
}
