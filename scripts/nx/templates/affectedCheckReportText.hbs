{{{prepend}}}
Check affected projects

{{#each statuses}}
{{#unless this.private}}
  - [{{#if this.readyToPublish}}OK{{else}}KO{{/if}}] {{{this.project}}}: Package {{{this.name}}}@{{{this.version}}}
  {{#unless this.readyToPublish}}
    {{#if this.isPublished}}
    - Version in package.json file is already published
    {{/if}}
    {{#unless this.changelogUpdated}}
    - Version is not defined in CHANGELOG.md file</code>
    {{/unless}}
    {{#unless this.sonarConfigUpdated}}
    - Version in sonar-project.properties file is wrong
    {{/unless}}
  {{/unless}}
{{else}}
  - [  ] {{{this.project}}}: Private package
{{/unless}}
{{/each}}

{{#if ok}}
All affected projects are upgraded
{{else}}
Affected projects are not upgraded properly
{{/if}}
